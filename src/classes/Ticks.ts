class Ticks {
    
    constructor() {
        //код конструктора
    }

    generateTicks(seriesData: number[], min: number, max: number, distributeFunc: any ): number[] {
        let ticks: number[];
        //код генерации массива "тиков"
        return ticks
    }

    drawTicks(ticks: number[], ctx: CanvasRenderingContext2D, labelPrintCallback: any, tickDrawCallback: any, ...coordinates: number[] ) {
        //функция отрисовки "тиков" на canvas
    }

  }